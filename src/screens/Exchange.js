import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Exchange = () => {
  return (
    <View>
      <Text>Exchange</Text>
    </View>
  );
};

export default memo(Exchange);
