import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Exchanges = () => {
  return (
    <View>
      <Text>Exchanges</Text>
    </View>
  );
};

export default memo(Exchanges);
