import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Wallets = () => {
  return (
    <View>
      <Text>Wallets</Text>
    </View>
  );
};

export default memo(Wallets);
